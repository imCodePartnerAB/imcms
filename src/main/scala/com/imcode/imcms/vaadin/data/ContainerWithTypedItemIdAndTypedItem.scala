package com.imcode.imcms.vaadin.data

import com.vaadin.data.{Item, Container}


//trait ContainerWithTypedItemIdAndTypedItem[A <: TItemId, B <: Item] extends ContainerWithTypedItemId[A] { container: Container =>
//  override def item(id: A): B //= getItem(id).asInstanceOf[B]
//  override def getItem(itemId: AnyRef): B = super.item(itemId).asInstanceOf[B]
//}
