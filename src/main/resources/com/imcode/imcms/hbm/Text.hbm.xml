<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE hibernate-mapping PUBLIC "-//Hibernate/Hibernate Mapping DTD 3.0//EN"
    "http://hibernate.sourceforge.net/hibernate-mapping-3.0.dtd">

<hibernate-mapping>

  <query name="Text.deleteTextsByDocRefAndLanguage">
    <![CDATA[
    DELETE FROM Text t WHERE t.docRef = :docRef AND t.language = :language
    ]]>
  </query>

  <!-- Collection -->
  <query name="Text.getByDocRefAndLanguage">
    <![CDATA[
    SELECT t FROM Text t WHERE t.docRef = :docRef AND t.language = :language
    ]]>
  </query>

  <!-- Collection -->
  <query name="Text.getByDocRef">
    <![CDATA[
    SELECT t FROM Text t WHERE t.docRef = :docRef
    ]]>
  </query>

  <!-- Unique result -->
  <query name="Text.getByDocRefAndNoAndLanguage">
    <![CDATA[
    SELECT t FROM Text t WHERE t.docRef = :docRef AND t.no = :no AND t.language = :language
    ]]>
  </query>

</hibernate-mapping>