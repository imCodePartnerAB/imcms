package com.imcode.imcms.servlet.tags;

import com.imcode.imcms.api.TextDocumentViewing;
import com.imcode.imcms.api.TextDocument;

import javax.servlet.jsp.tagext.TagSupport;
import javax.servlet.jsp.JspException;
import javax.servlet.http.HttpServletRequest;

public class TextFieldTag extends TagSupport {
/*
    public int doStartTag() throws JspException {
        TextDocumentViewing view = TextDocumentViewing.fromRequest((HttpServletRequest) pageContext.getRequest());
        if (null != view) {
            TextDocument textDocument = view.getTextDocument();
            if (null != textDocument) {
            }
        }
    }
*/
}
