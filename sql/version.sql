CREATE TABLE meta_versions (

CREATE TABLE document_versions (
  rid,
  meta_id,
  version
)

CREATE TABLE metas_current_versions (
CREATE TABLE document_current_versions (
  rid,
  meta_id,
  version
)

TABLE texts, images, etc

revision <- text revision nr ???
document_version <- 
